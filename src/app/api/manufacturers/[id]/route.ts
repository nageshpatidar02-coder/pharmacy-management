import { NextResponse } from "next/server";
import { manufacturerSchema } from "@/lib/validations/medicine";
import { requirePermission, PERMISSIONS } from "@/server/auth/permissions";
import { prisma } from "@/server/db/prisma";

export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { try { await requirePermission(PERMISSIONS.medicineCreate); const { id } = await params; return NextResponse.json(await prisma.manufacturer.update({ where: { id }, data: manufacturerSchema.parse(await request.json()) })); } catch (error) { const code = (error as { code?: string }).code; return NextResponse.json({ error: code === "P2025" ? "Manufacturer not found." : code === "P2002" ? "A manufacturer with this name already exists." : error instanceof Error ? error.message : "Unable to update manufacturer." }, { status: code === "P2025" ? 404 : code === "P2002" ? 409 : 400 }); } }
export async function DELETE(_request: Request, { params }: { params: Promise<{ id: string }> }) { try { await requirePermission(PERMISSIONS.medicineDelete); const { id } = await params; return NextResponse.json(await prisma.manufacturer.update({ where: { id }, data: { active: false } })); } catch (error) { const code = (error as { code?: string }).code; return NextResponse.json({ error: code === "P2025" ? "Manufacturer not found." : "Unable to deactivate manufacturer safely." }, { status: code === "P2025" ? 404 : 400 }); } }
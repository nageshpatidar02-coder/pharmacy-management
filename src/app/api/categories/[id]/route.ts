import { NextResponse } from "next/server";
import { categorySchema } from "@/lib/validations/medicine";
import { requirePermission, PERMISSIONS } from "@/server/auth/permissions";
import { prisma } from "@/server/db/prisma";

export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { try { await requirePermission(PERMISSIONS.medicineCreate); const { id } = await params; return NextResponse.json(await prisma.category.update({ where: { id }, data: categorySchema.parse(await request.json()) })); } catch (error) { const code = (error as { code?: string }).code; return NextResponse.json({ error: code === "P2025" ? "Category not found." : code === "P2002" ? "A category with this name already exists." : error instanceof Error ? error.message : "Unable to update category." }, { status: code === "P2025" ? 404 : code === "P2002" ? 409 : 400 }); } }
export async function DELETE(_request: Request, { params }: { params: Promise<{ id: string }> }) { try { await requirePermission(PERMISSIONS.medicineDelete); const { id } = await params; return NextResponse.json(await prisma.category.update({ where: { id }, data: { active: false } })); } catch (error) { const code = (error as { code?: string }).code; return NextResponse.json({ error: code === "P2025" ? "Category not found." : "Unable to deactivate category safely." }, { status: code === "P2025" ? 404 : 400 }); } }
import Link from "next/link";

import { AppShell } from "@/components/layout/app-shell";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { requirePermission, PERMISSIONS } from "@/server/auth/permissions";
import { listPurchases } from "@/server/services/purchase.service";

export default async function PurchasesPage() { const user = await requirePermission(PERMISSIONS.purchaseView); const purchases = await listPurchases(); return <AppShell user={user}><div className="mx-auto max-w-6xl space-y-6"><div className="flex items-end justify-between"><div><p className="text-sm font-semibold uppercase tracking-[0.18em] text-primary">Purchasing</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Purchase history</h1></div><Link href="/purchases/new" className="rounded-md bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground">New purchase</Link></div><Card><CardHeader><CardTitle>Completed purchases</CardTitle></CardHeader><CardContent>{purchases.length === 0 ? <p className="py-10 text-center text-sm text-muted-foreground">No purchases recorded yet.</p> : <div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b text-muted-foreground"><th className="pb-3">Invoice</th><th className="pb-3">Supplier</th><th className="pb-3">Date</th><th className="pb-3">Total</th><th className="pb-3">Paid</th><th className="pb-3">Balance</th></tr></thead><tbody>{purchases.map((purchase) => <tr key={purchase.id} className="border-b last:border-0"><td className="py-3 font-medium">{purchase.invoiceNumber}</td><td className="py-3">{purchase.supplier.businessName}</td><td className="py-3">{purchase.invoiceDate.toLocaleDateString()}</td><td className="py-3">{purchase.grandTotal.toFixed(2)}</td><td className="py-3">{purchase.paidAmount.toFixed(2)}</td><td className="py-3">{purchase.balanceAmount.toFixed(2)}</td></tr>)}</tbody></table></div>}</CardContent></Card></div></AppShell>; }

import { notFound } from "next/navigation";

import { AppShell } from "@/components/layout/app-shell";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { requirePermission, PERMISSIONS } from "@/server/auth/permissions";
import { prisma } from "@/server/db/prisma";

export default async function SupplierDetailPage({ params }: { params: Promise<{ id: string }> }) { const user = await requirePermission(PERMISSIONS.suppliersView); const { id } = await params; const supplier = await prisma.supplier.findUnique({ where: { id }, include: { purchases: { orderBy: { invoiceDate: "desc" }, take: 20 }, payments: { orderBy: { createdAt: "desc" }, take: 20 } } }); if (!supplier) notFound(); return <AppShell user={user}><div className="mx-auto max-w-6xl space-y-6"><div><p className="text-sm font-semibold uppercase tracking-[0.18em] text-primary">Supplier</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">{supplier.businessName}</h1><p className="mt-2 text-muted-foreground">{supplier.contactPerson ?? "No contact person"} · {supplier.mobile ?? "No mobile"}</p></div><div className="grid gap-4 sm:grid-cols-3"><Card><CardContent className="pt-6"><p className="text-sm text-muted-foreground">Outstanding balance</p><p className="mt-2 text-2xl font-semibold">{supplier.outstandingBalance.toFixed(2)}</p></CardContent></Card><Card><CardContent className="pt-6"><p className="text-sm text-muted-foreground">Opening balance</p><p className="mt-2 text-2xl font-semibold">{supplier.openingBalance.toFixed(2)}</p></CardContent></Card><Card><CardContent className="pt-6"><p className="text-sm text-muted-foreground">Payment terms</p><p className="mt-2 text-2xl font-semibold">{supplier.paymentTerms ?? "-"}</p></CardContent></Card></div><div className="grid gap-6 xl:grid-cols-2"><Card><CardHeader><CardTitle>Purchase history</CardTitle></CardHeader><CardContent>{supplier.purchases.length === 0 ? <p className="text-sm text-muted-foreground">No purchases yet.</p> : <div className="space-y-3">{supplier.purchases.map((purchase) => <div key={purchase.id} className="flex justify-between border-b pb-3 text-sm"><span>{purchase.invoiceNumber}<br /><span className="text-xs text-muted-foreground">{purchase.invoiceDate.toLocaleDateString()}</span></span><span className="font-semibold">{purchase.grandTotal.toFixed(2)}</span></div>)}</div>}</CardContent></Card><Card><CardHeader><CardTitle>Payments</CardTitle></CardHeader><CardContent>{supplier.payments.length === 0 ? <p className="text-sm text-muted-foreground">No payments yet.</p> : <div className="space-y-3">{supplier.payments.map((payment) => <div key={payment.id} className="flex justify-between border-b pb-3 text-sm"><span>{payment.method}<br /><span className="text-xs text-muted-foreground">{payment.createdAt.toLocaleDateString()}</span></span><span className="font-semibold">{payment.amount.toFixed(2)}</span></div>)}</div>}</CardContent></Card></div></div></AppShell>; }

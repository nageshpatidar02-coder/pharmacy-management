import Link from "next/link";

import { AppShell } from "@/components/layout/app-shell";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { requirePermission, PERMISSIONS } from "@/server/auth/permissions";
import { listSuppliers } from "@/server/services/supplier.service";

export default async function SuppliersPage() { const user = await requirePermission(PERMISSIONS.suppliersView); const suppliers = await listSuppliers(); return <AppShell user={user}><div className="mx-auto max-w-6xl space-y-6"><div className="flex items-end justify-between"><div><p className="text-sm font-semibold uppercase tracking-[0.18em] text-primary">Purchasing</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Suppliers</h1><p className="mt-2 text-muted-foreground">Manage supplier balances and purchase relationships.</p></div><Link href="/suppliers/new" className="rounded-md bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground">Add supplier</Link></div><Card><CardHeader><CardTitle>Supplier directory</CardTitle></CardHeader><CardContent>{suppliers.length === 0 ? <p className="py-10 text-center text-sm text-muted-foreground">No suppliers recorded yet.</p> : <div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b text-muted-foreground"><th className="pb-3">Business</th><th className="pb-3">Contact</th><th className="pb-3">Mobile</th><th className="pb-3">Purchases</th><th className="pb-3">Outstanding</th><th className="pb-3">Status</th></tr></thead><tbody>{suppliers.map((supplier) => <tr key={supplier.id} className="border-b last:border-0"><td className="py-3 font-medium"><Link className="text-primary hover:underline" href={`/suppliers/${supplier.id}`}>{supplier.businessName}</Link></td><td className="py-3">{supplier.contactPerson ?? "-"}</td><td className="py-3">{supplier.mobile ?? "-"}</td><td className="py-3">{supplier._count.purchases}</td><td className="py-3">{supplier.outstandingBalance.toFixed(2)}</td><td className="py-3">{supplier.status}</td></tr>)}</tbody></table></div>}</CardContent></Card></div></AppShell>; }

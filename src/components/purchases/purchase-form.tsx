"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { purchaseSchema } from "@/lib/validations/purchase";

type Supplier = { id: string; businessName: string };
type Medicine = { id: string; name: string; barcode: string | null; purchasePrice: number; mrp: number; sellingPrice: number; gstPercentage: number };
type PurchaseLine = { medicineId: string; batchNumber: string; manufacturingDate: string; expiryDate: string; quantity: number; freeQuantity: number; purchaseRate: number; mrp: number; sellingPrice: number; discount: number; gstPercentage: number };
const emptyLine = (): PurchaseLine => ({ medicineId: "", batchNumber: "", manufacturingDate: "", expiryDate: "", quantity: 1, freeQuantity: 0, purchaseRate: 0, mrp: 0, sellingPrice: 0, discount: 0, gstPercentage: 0 });
const money = (value: number) => Math.round((value + Number.EPSILON) * 100) / 100;

export function PurchaseForm({ suppliers, medicines }: { suppliers: Supplier[]; medicines: Medicine[] }) {
  const router = useRouter();
  const [lines, setLines] = useState<PurchaseLine[]>([emptyLine()]);
  const [supplierId, setSupplierId] = useState("");
  const [invoiceNumber, setInvoiceNumber] = useState("");
  const [invoiceDate, setInvoiceDate] = useState(new Date().toISOString().slice(0, 10));
  const [dueDate, setDueDate] = useState("");
  const [paymentMethod, setPaymentMethod] = useState("CREDIT");
  const [paidAmount, setPaidAmount] = useState(0);
  const [discountType, setDiscountType] = useState<"AMOUNT" | "PERCENTAGE">("AMOUNT");
  const [discountValue, setDiscountValue] = useState(0);
  const [barcode, setBarcode] = useState("");
  const [error, setError] = useState("");
  const [pending, setPending] = useState(false);

  const totals = lines.reduce((result, line) => {
    const gross = line.quantity * line.purchaseRate;
    const lineDiscount = Math.min(Math.max(line.discount, 0), gross);
    const taxable = Math.max(gross - lineDiscount, 0);
    const tax = taxable * line.gstPercentage / 100;
    result.subtotal += gross; result.lineDiscount += lineDiscount; result.taxable += taxable; result.tax += tax;
    return result;
  }, { subtotal: 0, lineDiscount: 0, taxable: 0, tax: 0 });
  const billDiscount = discountType === "PERCENTAGE" ? Math.min(totals.taxable * Math.min(discountValue, 100) / 100, totals.taxable) : Math.min(Math.max(discountValue, 0), totals.taxable);
  const grandTotal = Math.round(Math.max(totals.taxable - billDiscount, 0) + totals.tax);
  const totalDiscount = totals.lineDiscount + billDiscount;
  const dueAmount = Math.max(grandTotal - paidAmount, 0);
  const paymentStatus = paidAmount <= 0 ? "UNPAID" : paidAmount >= grandTotal ? "PAID" : "PARTIAL";

  function updateLine(index: number, patch: Partial<PurchaseLine>) { setLines((current) => current.map((line, lineIndex) => lineIndex === index ? { ...line, ...patch } : line)); }
  function selectMedicine(index: number, medicineId: string) { const medicine = medicines.find((entry) => entry.id === medicineId); updateLine(index, { medicineId, purchaseRate: medicine?.purchasePrice ?? 0, mrp: medicine?.mrp ?? 0, sellingPrice: medicine?.sellingPrice ?? 0, gstPercentage: medicine?.gstPercentage ?? 0 }); }
  function lookupBarcode() { const medicine = medicines.find((entry) => entry.barcode === barcode.trim()); if (!medicine) { setError("No medicine found for this barcode. Add it to Medicine Master first."); return; } selectMedicine(0, medicine.id); setBarcode(""); setError(""); }

  async function submit(event: React.FormEvent<HTMLFormElement>) {
    event.preventDefault(); setPending(true); setError("");
    if (paidAmount > grandTotal) { setError(`Paid amount cannot exceed payable amount ${grandTotal.toFixed(2)}.`); setPending(false); return; }
    const parsed = purchaseSchema.safeParse({ supplierId, invoiceNumber, invoiceDate, dueDate: dueDate || undefined, paymentMethod, paidAmount, discountType, discountValue, items: lines });
    if (!parsed.success) { setError(parsed.error.issues[0]?.message ?? "Check the purchase details."); setPending(false); return; }
    try { const response = await fetch("/api/purchases", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(parsed.data) }); const result = await response.json().catch(() => ({})); if (!response.ok) { setError(result.error ?? "Unable to complete purchase."); return; } router.push("/purchases"); router.refresh(); } catch (requestError) { setError(requestError instanceof Error ? requestError.message : "Unable to connect to the server."); } finally { setPending(false); }
  }

  return <form onSubmit={submit} className="space-y-6"><div className="grid gap-4 rounded-xl border bg-surface p-6 md:grid-cols-4"><label className="text-sm font-medium">Supplier<select className="mt-2 h-10 w-full rounded-md border bg-background px-3" value={supplierId} onChange={(event) => setSupplierId(event.target.value)} required><option value="">Select supplier</option>{suppliers.map((supplier) => <option key={supplier.id} value={supplier.id}>{supplier.businessName}</option>)}</select></label><label className="text-sm font-medium">Invoice number<Input value={invoiceNumber} onChange={(event) => setInvoiceNumber(event.target.value)} required /></label><label className="text-sm font-medium">Purchase date<Input type="date" value={invoiceDate} onChange={(event) => setInvoiceDate(event.target.value)} required /></label><label className="text-sm font-medium">Due date<Input type="date" value={dueDate} onChange={(event) => setDueDate(event.target.value)} /></label></div><div className="rounded-xl border bg-surface p-6"><div className="mb-4 flex flex-wrap items-end justify-between gap-3"><div><h2 className="font-semibold">Purchase items</h2><p className="mt-1 text-xs text-muted-foreground">Medicine selection fills existing rate and GST; every total updates as you type.</p></div><div className="flex gap-2"><Input aria-label="Medicine barcode" placeholder="Scan barcode" value={barcode} onChange={(event) => setBarcode(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter") { event.preventDefault(); lookupBarcode(); } }} /><Button type="button" variant="outline" onClick={lookupBarcode}>Find barcode</Button><Button type="button" variant="outline" onClick={() => setLines((current) => [...current, emptyLine()])}>Add item</Button></div></div><div className="space-y-4">{lines.map((line, index) => { const gross = line.quantity * line.purchaseRate; const lineDiscount = Math.min(line.discount, gross); const tax = Math.max(gross - lineDiscount, 0) * line.gstPercentage / 100; const lineTotal = gross - lineDiscount + tax; return <div key={index} className="grid gap-3 rounded-lg border bg-background p-4 md:grid-cols-4"><label className="text-xs font-medium md:col-span-2">Medicine<select className="mt-1 h-10 w-full rounded-md border bg-surface px-3" value={line.medicineId} onChange={(event) => selectMedicine(index, event.target.value)} required><option value="">Select medicine</option>{medicines.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><Field label="Batch number" value={line.batchNumber} onChange={(value) => updateLine(index, { batchNumber: value })} required /><Field label="Quantity" value={line.quantity} onChange={(value) => updateLine(index, { quantity: Math.max(1, Number(value) || 0) })} type="number" required /><Field label="Free quantity" value={line.freeQuantity} onChange={(value) => updateLine(index, { freeQuantity: Math.max(0, Number(value) || 0) })} type="number" /><Field label="Purchase rate" value={line.purchaseRate} onChange={(value) => updateLine(index, { purchaseRate: Math.max(0, Number(value) || 0) })} type="number" required /><Field label="MRP" value={line.mrp} onChange={(value) => updateLine(index, { mrp: Math.max(0, Number(value) || 0) })} type="number" required /><Field label="Selling price" value={line.sellingPrice} onChange={(value) => updateLine(index, { sellingPrice: Math.max(0, Number(value) || 0) })} type="number" required /><Field label="Line discount" value={line.discount} onChange={(value) => updateLine(index, { discount: Math.max(0, Number(value) || 0) })} type="number" /><Field label="GST %" value={line.gstPercentage} onChange={(value) => updateLine(index, { gstPercentage: Math.min(100, Math.max(0, Number(value) || 0)) })} type="number" /><Field label="Manufacturing date" value={line.manufacturingDate} onChange={(value) => updateLine(index, { manufacturingDate: value })} type="date" required /><Field label="Expiry date" value={line.expiryDate} onChange={(value) => updateLine(index, { expiryDate: value })} type="date" required /><div className="flex items-end justify-between gap-2 text-sm"><span>Line total<br /><strong>{money(lineTotal).toFixed(2)}</strong></span><Button type="button" variant="ghost" size="sm" disabled={lines.length === 1} onClick={() => setLines((current) => current.filter((_, lineIndex) => lineIndex !== index))}>Remove</Button></div></div>; })}</div></div><div className="grid gap-4 md:grid-cols-[1fr_380px]"><div className="rounded-xl border bg-surface p-6"><h2 className="font-semibold">Discount and payment</h2><div className="mt-4 grid gap-3 md:grid-cols-2"><label className="text-sm font-medium">Discount type<select value={discountType} onChange={(event) => setDiscountType(event.target.value as "AMOUNT" | "PERCENTAGE")} className="mt-2 h-10 w-full rounded-md border bg-background px-3"><option value="AMOUNT">Amount</option><option value="PERCENTAGE">Percentage</option></select></label><label className="text-sm font-medium">Discount value<Input type="number" min="0" max={discountType === "PERCENTAGE" ? 100 : undefined} step="0.01" value={discountValue} onChange={(event) => setDiscountValue(Math.max(0, Number(event.target.value) || 0))} /></label><label className="text-sm font-medium">Payment method<select value={paymentMethod} onChange={(event) => setPaymentMethod(event.target.value)} className="mt-2 h-10 w-full rounded-md border bg-background px-3"><option>CASH</option><option>UPI</option><option>CARD</option><option>BANK</option><option>CREDIT</option></select></label><label className="text-sm font-medium">Paid amount<Input type="number" min="0" max={grandTotal} step="0.01" value={paidAmount} onChange={(event) => setPaidAmount(Math.max(0, Number(event.target.value) || 0))} /></label></div></div><div className="rounded-xl border bg-surface p-6 text-sm"><div className="flex justify-between"><span>Subtotal</span><strong>{money(totals.subtotal).toFixed(2)}</strong></div><div className="mt-2 flex justify-between"><span>GST</span><strong>{money(totals.tax).toFixed(2)}</strong></div><div className="mt-2 flex justify-between"><span>Total discount</span><strong>{money(totalDiscount).toFixed(2)}</strong></div><div className="mt-3 flex justify-between border-t pt-3 text-lg font-semibold"><span>Grand total</span><span>{grandTotal.toFixed(2)}</span></div><div className="mt-2 flex justify-between text-emerald-700"><span>Paid</span><span>{money(paidAmount).toFixed(2)}</span></div><div className="mt-2 flex justify-between font-semibold text-amber-700"><span>Due</span><span>{dueAmount.toFixed(2)}</span></div><div className="mt-2 flex justify-between"><span>Status</span><strong className={paymentStatus === "PAID" ? "text-emerald-700" : paymentStatus === "PARTIAL" ? "text-amber-700" : "text-red-700"}>{paymentStatus}</strong></div></div></div><Button disabled={pending}>{pending ? "Saving purchase..." : "Save purchase bill"}</Button>{error && <p role="alert" className="text-sm text-red-600">{error}</p>}</form>;
}

function Field({ label, value, onChange, type = "text", required }: { label: string; value: string | number; onChange: (value: string) => void; type?: string; required?: boolean }) { return <label className="text-xs font-medium">{label}<Input className="mt-1 bg-surface" type={type} value={value} onChange={(event) => onChange(event.target.value)} required={required} /></label>; }

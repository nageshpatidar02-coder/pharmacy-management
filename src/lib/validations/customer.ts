import { z } from "zod";
export const customerSchema = z.object({ name: z.string().trim().min(2).max(160), mobile: z.string().trim().regex(/^[0-9+()\-\s]{7,20}$/, "Enter a valid mobile number").optional().default(""), email: z.union([z.string().email(), z.literal("")]).optional().default(""), address: z.string().trim().max(500).optional().default(""), openingBalance: z.coerce.number().finite().min(0).default(0) });
